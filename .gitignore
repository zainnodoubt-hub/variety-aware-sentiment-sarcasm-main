# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Virtual environment / caches (recreate from requirements.txt) ---
nlpenv/
.pip_cache/
.gradio/
.cache/

# --- Large model artifacts (regenerated by training; not for version control) ---
# 37 GB of encoder checkpoints + Qwen LoRA adapters. All results are preserved
# in outputs/tables/07_ALL_EXPERIMENTS_SUMMARY.csv, so the repo stays reproducible
# without them.
checkpoints/

# --- Graded coursework artifacts kept local, not published ---
# The repo is the clean script-based version; the original notebook and the
# as-submitted bundle stay on the local machine only.
main.ipynb
submission/

# --- Editor / OS noise ---
.~lock.*#
.DS_Store
*.swp
